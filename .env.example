# https://platform.openai.com/api-keys
API=
# https://platform.openai.com/docs/models
MODEL=
# https://platform.openai.com/docs/models/embeddings
MODEL_EMBEDDING=
# Base URL for custom embedding API (uses BASE_URL if not provided)
EMBEDDING_BASE_URL=
# API key for embedding model (uses main API key if not provided)
# Set to "false" to disable API key authentication for embedding
# When set to "false", no API key will be used for embedding requests
EMBEDDING_API_KEY=
# https://platform.openai.com/docs/guides/text-generation/managing-tokens
maxTokens=400
# https://platform.openai.com/docs/guides/text-generation/how-should-i-set-the-temperature-parameter
temperature=0.7
# Base URL for custom API/localhost an AI model server 
BASE_URL=
